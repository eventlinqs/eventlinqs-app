# Lighthouse CI gate - enforces docs/MEDIA-ARCHITECTURE.md §12.2.
#
# Runs on every PR. Boots a local Supabase stack so the dynamic routes
# in lighthouserc.json (/events/browse/[city], /events/[slug],
# /categories/[slug]) resolve to real seeded content, builds the
# Next.js app, then audits the URL set against the regression-gate
# thresholds in lighthouserc.json.
#
# Gate state (activated 2026-04-27 after Pre-Task 3):
#   - Hard gate (continue-on-error: false). PRs failing this gate
#     cannot merge.
#   - Thresholds are calibrated to the iter-6 simulator floor on
#     this stack: Performance >= 0.65, A11y >= 0.95, BP = 1.0,
#     SEO >= 0.9, CLS <= 0.05. LCP/TBT/FCP/SI are warn-only because
#     they are dominated by the Lighthouse simulator's 4x CPU model.
#     Real-device CWV measurement on Vercel deploy will be added as
#     a separate gate in Pre-Task 4.

name: Lighthouse CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: lighthouse-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lighthouse:
    name: Lighthouse mobile gate
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      NEXT_TELEMETRY_DISABLED: 1
      NEXT_PUBLIC_SITE_URL: https://eventlinqs.com

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      # Boot a local Supabase stack so dynamic Lighthouse URLs
      # (/events/melbourne, /events/bollywood-nights-...) resolve to
      # real content. `supabase start` applies every file in
      # supabase/migrations/ including the cultural breadth seed.
      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start Supabase
        run: supabase start

      - name: Export Supabase env
        # NEXT_PUBLIC_* must be present BEFORE `next build` so the values
        # are inlined into the client bundle. Pull them from the running
        # local stack and write to $GITHUB_ENV for subsequent steps.
        run: |
          status=$(supabase status -o env)
          api_url=$(echo "$status" | grep '^API_URL=' | cut -d= -f2- | tr -d '"')
          anon_key=$(echo "$status" | grep '^ANON_KEY=' | cut -d= -f2- | tr -d '"')
          echo "NEXT_PUBLIC_SUPABASE_URL=$api_url" >> $GITHUB_ENV
          echo "NEXT_PUBLIC_SUPABASE_ANON_KEY=$anon_key" >> $GITHUB_ENV

      - name: Build
        run: npm run build

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          configPath: ./lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true
