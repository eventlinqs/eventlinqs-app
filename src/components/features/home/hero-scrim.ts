/**
 * THE ONE HERO SCRIM. The bottom-up navy wash that sits between every homepage
 * hero photograph and its text, so the headline, the detail line and the gold
 * call to action clear 4.5 to 1 on any image in the set (Design system: every
 * hero shares one treatment; C17.4 measures it on the captures).
 *
 * Defined once here since close-out C17 (7 September 2026) because the carousel
 * and the no-event hero paint the same scrim, and a scrim tuned in one place and
 * copied to another is how the C3 scrim defect happened.
 */
export const HERO_SCRIM_GRADIENT =
  'linear-gradient(to top, rgba(10,22,40,0.84) 0%, rgba(10,22,40,0.54) 20%, rgba(10,22,40,0.24) 44%, rgba(10,22,40,0.06) 68%, rgba(10,22,40,0) 88%)'
