/**
 * THE ONE ATTRIBUTE THE TICKET SELECTOR FINDS THE DISCOVERY BOX BY.
 *
 * A LEAF ON PURPOSE. The panel that renders the box is a server component, and
 * a client component importing any member of that module would pull the whole
 * component, its JSX and its imports into the first load of every event page,
 * which is the exact shape lane A measured and removed from the dashboard shell
 * on 19 September (one avatar size literal dragging every image hint on the
 * platform with it). One string, no imports, nothing to drag.
 */
export const DISCOVERY_CONSENT_ATTRIBUTE = 'data-discovery-consent'
