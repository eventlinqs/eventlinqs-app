# M4.5 close-out audit: Steps E and A2 (hardening item 11)

Audited 2026-06-06. Pre-launch hardening item 11 reads "M4.5 Close-out Steps E
and A2". This records their actual status with evidence.

## Source of truth

`docs/MODULE-4.5-MANIFEST.md` defines the M4.5 close-out as four streams:

- STREAM A - Foundation fixes: Tasks A1, A2, A3, A4
- STREAM B - Homepage synchronisation: Tasks B1 to B5
- STREAM C - Event detail, auth, and dashboard synchronisation
- STREAM D - Pre-launch polish: Tasks D1 (lint/type/build), D2 (Vercel
  preview), D3 (Lighthouse), D4 (merge, tag, deploy)

The `m4.5-close-out` branch has since merged to `main` (it was 0 commits ahead
of `main` and was deleted in the 2026-06-06 housekeeping pass), so the streams
landed.

## Task A2 - Sweep unicode escape literals  -  DONE

Definition (manifest Task A2): remove `\uXXXX` escape literals across `src/`;
acceptance test `grep -rn "\u20" src/` returns zero results and no em-dashes or
en-dashes appear in user-facing copy.

Evidence (re-run 2026-06-06):

- `grep -rn '\u20' src/` -> 0 results
- literal em-dash `U+2014` in `src/` -> 0
- literal en-dash `U+2013` in `src/` -> 0

The acceptance test passes. A2 is code-side and already satisfied; no change
needed. The dash rule is additionally locked by the brand-voice conventions and
the new CLAUDE.md.

## "Step E"  -  NOT DEFINED in the repo

There is no Task E, Stream E, or "Step E" in `docs/MODULE-4.5-MANIFEST.md`; the
streams run A through D only. The label in hardening item 11 does not resolve to
any documented M4.5 step, so there is nothing concrete to verify or complete
against.

What can be said:

- The M4.5 close-out as documented (Streams A to D) is merged to `main`.
- Stream D's steps (lint/type/build, Vercel preview, Lighthouse, merge/tag/
  deploy) are operational/deploy steps, not code deliverables, and the work
  they gate is on `main`.

Recommendation for the founder: confirm what "Step E" referred to (it may be
shorthand from an out-of-repo planning note). If it maps to a Stream D deploy/
verify step, that is operational and already on `main`; if it is a distinct
code task, point to its definition and it can be completed. Absent a
definition, no code change is made for "E" rather than guessing.

## Net status for hardening item 11

- A2: DONE (verified, evidence above).
- E: cannot be actioned - undefined in the repo; founder clarification needed.
