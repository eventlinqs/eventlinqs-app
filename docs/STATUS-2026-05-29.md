# EventLinqs: Status Handoff for New Chat

Date: 29 May 2026
Purpose: Single source of truth for a new Claude chat to read first and pick up exactly where the previous chat left off, without Lawal having to re-explain.

Founder reading this doc to a new Claude: paste the prompt at the end of this doc into the new chat as your first message. The new Claude will read this file, sync to your locked memory entries, and continue.

---

## 1. Who and what

Lawal Adams, sole trader, ABN 30 837 447 587, Geelong VIC. Australian citizen (locked in memory permanently, no visa or immigration constraints). Building EventLinqs (eventlinqs.com), a community-first national ticketing platform for Australia. Pre-launch.

Positioning: community-first, NOT culture-first. Platform open to all events. Tagline "Every culture. Every event. One platform."

## 2. Tools and environment in use

- Stack: Next.js 16.2.2 (Turbopack), React, Supabase Sydney (project gndnldyfudbytbboxesk), Tailwind v4, Stripe (TEST mode currently), Resend, Upstash Redis, Mapbox, Vercel Pro.
- Production: https://www.eventlinqs.com
- Repo: github.com/eventlinqs/eventlinqs-app
- Worktrees on Lawal's machine: C:\Users\61416\OneDrive\Desktop\EventLinqs\eventlinqs-app and variants (-tab-a, -tab-b, -tab-c, -hardening, -sentry-fix, -admin)
- Three Claude Code (CC) tabs running concurrently: Tab A, Tab B, Tab C. Each has bypass permissions on (--dangerously-skip-permissions).
- CC just upgraded to Claude Opus 4.8 (released 28 May 2026), effort high (default), via `/model` then `/effort high` in each tab.
- Sentry: working in production, verified, capturing errors with PII scrubbing.

## 3. Designer

Rizwan Ali (Pakistan), Upwork contract live, milestone 1 ($175 USD) paid, milestone 2 due on full Figma delivery. Currently on Eid break - DO NOT message during Eid. When he returns (a few days), send a scope addendum BEFORE milestone 2 starts that adds: browse/discovery page (filter-first, genre nav) + genre-chip + artist-chip components. This addendum is CRITICAL for the genre moat foundation. Lawal hates scope changes - keep the addendum tight and minimal. Quality target: 9/10 vs Humanitix, Ticketmaster, DICE, Eventbrite. NON-NEGOTIABLE.

## 4. Strategy (the MOAT plan, locked)

Moats are EARNED post-launch, NOT pre-built. Build foundations now, ship, compound. Sequence:

1. Harden existing system (in progress)
2. Build moat foundations: genre taxonomy, filter-first browse page, artist model, artist chips, guest checkout, GA/tiered tickets, follow-feed
3. National launch, concierge first 10 organisers
4. Fast-follow post-launch: community/hyper-specific tags, curator/DJ ambassador collections, Bandsintown/Spotify event sync
5. DEFER reserved seating until a real organiser needs it

Differentiator vs Eventbrite: they limit sub-genre browse to US only. EventLinqs launches with full AU sub-genre browse from day one. That's a real "better than them" claim on home turf.

Genre Discovery Foundation Spec already written and saved at `docs/GENRE-DISCOVERY-FOUNDATION-SPEC.md`. Read it for the build plan. CC builds the data layer now (taxonomy, artist model, follow model, URL/SEO structure). The visual browse page UI waits for Rizwan's Figma. CC does NOT improvise design - that rule is non-negotiable.

## 5. What is DONE (don't re-do)

- Sentry observability working in production (PRs #48, #49, #50)
- ACL legal exposures removed from /help (PR #46)
- Payment audit doc persisted: audit-v2/AUDIT-PAYMENT-2026-05-26.md
- Homepage Lighthouse carve-out (PR #48)
- PR #51 merged: pricing_rules schema-drift fix (the bug that crashed checkout). Commit 95a4775.
- PR #52 merged: regenerated database.ts from live schema (auto-generated, not handwritten), 12 hidden drift bugs fixed, types-drift CI guard added (non-blocking via continue-on-error until SUPABASE_ACCESS_TOKEN secret is added). Commit 36d73c3c. Production deployment dpl_FgVFqkLmDuGgENMDRnTE3Kh7tpWh.
- PO Box 141, NEWCOMB VIC 3219 secured (Australia Post order 40000019238897)
- All 5 social platforms registered: LinkedIn (/company/123414000), Facebook (id=61590528656810), Instagram, X, TikTok
- Logo placeholder PNGs created (300, 500, 1080 sizes), navy #0A1628 + gold #D4A437
- Designer Rizwan engaged, paid milestone 1, NDA signed
- Memory updated with citizenship, MOAT strategy, designer scope addition, all social URLs

## 6. What is IN FLIGHT or NEXT (pick up from here)

### Immediate next action: complete Stripe Connect onboarding for ONE test organiser

State: zero organisers in production have completed Stripe Connect onboarding. All 17 orgs have stripe_account_id: null. That's why the checkout test on 29 May showed "This organiser has not finished payment setup" - it's the system correctly refusing to take money for a seller with no connected bank account.

Lawal needs to do this himself in the browser (CC can't, it requires the org owner to physically walk through Stripe's hosted onboarding pages). Recommended test org: `phase2-test-org` (a throwaway test account). Stripe is in TEST mode, accepts dummy data.

Steps:
1. Sign into eventlinqs.com as the owner of phase2-test-org
2. Navigate to /dashboard/payouts
3. Click "Set up payouts"
4. Complete Stripe hosted onboarding with dummy data
5. Return to app, account.updated webhook auto-populates the org's Stripe fields
6. Re-run test purchase on any phase2-test-org event with Stripe test card 4242 4242 4242 4242

After that succeeds end-to-end (purchase completes, ticket QR renders at /t/[code], /tickets dashboard shows it, confirmation email arrives), the checkout flow is empirically proven and the platform can take real payments.

### Then: admin panel inventory

CC has this queued as a read-only task: what /admin functionality exists, what's scaffolded, what's not started. Run it after the purchase test succeeds.

### Then: comprehensive critical-path sweep

Verify every important user journey end-to-end: signup, login, browse, event detail, checkout, payment, ticket, dashboard, organiser creates event, refund. Find any remaining gaps before public launch.

### Outstanding cleanup (lower priority, before national launch)

- 3 remaining ACL surfaces: legal/refunds/page.tsx ("When EventLinqs Guarantees a Refund"), CheckoutTrustSignals.tsx, EventLinqs_Scope_v5.md tax-invoice line
- PO Box 141 NEWCOMB VIC 3219 onto /legal/terms, /legal/privacy, /legal/refunds, footer
- Resend domain migration Tokyo -> Sydney region
- Mapbox URL restrictions
- Reservation expiry sweeper (P3-2 from audit, still missing)
- Credential rotation pre-launch (Supabase DB password, Google Maps key, PSI key - values in docs/security/ not in memory)
- Lighthouse paths-ignore filter (stops docs-only PRs being blocked by Lighthouse)
- Adobe Stock trial cancellation by 29 May (today - check immediately)
- Stripe TEST -> LIVE mode (M6 Phase 3 task, before national launch only)
- SUPABASE_ACCESS_TOKEN secret optionally added to GitHub to flip types-drift guard from warn-level to blocking

## 7. Non-negotiable working rules

- No em-dashes or en-dashes anywhere (hyphens, colons, pipes only)
- Australian English
- No exclamation marks in copy
- Community-first language, never culture-first or diaspora-first
- CC follows Scope Manifests (authorised files / do-not-touch lists) - never improvise design, never touch unauthorised files
- All migrations via `supabase db push --linked` from PowerShell, never Dashboard SQL, never MCP apply_migration
- CI/PR standard: all checks green, no `gh pr merge --admin` except declared emergencies
- Robustness standard: every component works 24/7, zero broken states, verified end to end in production before called done
- Lawal is the founder and final decision-maker. Claude proposes options with a clear recommendation, Lawal chooses. Claude is the CTO/project manager, not a tool-use bot. Hold the line on quality WITH Lawal, not despite him.

## 8. Communication style Lawal prefers

- Direct and honest, no sugar-coating
- Visual-first when a diagram helps the point
- Tight prose, not bloated. Don't over-explain.
- One clear recommended next action at the end of each response
- When making him do something manually, explain why precisely AND offer to do anything in-house that doesn't strictly require him
- Distinguish CC's auto-generated suggestion lines from Lawal's actual instructions (he will tell you when something is auto vs his)
- Lawal gets frustrated when he hits the same problem repeatedly - explain pattern vs symptom, give the permanent fix not just the patch
- Acknowledge when a "Ticketmaster never breaks" expectation comes up: it does break (Taylor Swift presale 2022 etc), the realistic robust standard is "fails gracefully, alerts instantly, can't repeat known bugs" - which EventLinqs now has

## 9. New chat first-message prompt

When opening the new chat, paste this as the first message:

```
You are continuing work on EventLinqs, a pre-launch Australian ticketing platform I am building. Read this status file before doing anything else and continue from where the previous chat left off:

docs/STATUS-2026-05-29.md

After you have read it, give me a one-paragraph confirmation that you have the full picture, then ask me what I want to work on next. Default-recommend the Stripe Connect onboarding step (item 6, immediate next action) since that's the unblock for the empirical proof that payments work.

Do not invent context. If something here conflicts with what you find in your memory entries, the memory entries win unless they pre-date this file (this file is dated 29 May 2026). If anything is unclear, ask me.
```
