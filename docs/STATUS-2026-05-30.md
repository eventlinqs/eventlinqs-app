# EventLinqs: Status Handoff for New Chat

Date: 30 May 2026
Purpose: Single source of truth so a new Claude chat picks up exactly where the previous one left off. Founder (Lawal) pastes the first-message prompt at the end into the new chat.

IMPORTANT FOR THE NEW CLAUDE: You do NOT have live access to Lawal's machine or his Claude Code (CC) tabs. You give him prompts to paste into CC; he pastes results back. He is the bridge. Never assume you can see or control CC, his files, or his browser directly. Always give clear numbered steps. Whenever you send him to PowerShell, give the `cd` line for his working directory first. Whenever you tell him to start Claude Code, give the full command `claude --dangerously-skip-permissions`, never bare `claude`. When sending him into any manual flow (Stripe, forms), give EVERY exact value and click up front - do not make him come back to ask.

---

## 1. Who and what

Lawal Adams, sole trader, ABN 30 837 447 587, Geelong VIC, Australian citizen (no visa constraints). Building EventLinqs (eventlinqs.com), a community-first national Australian ticketing platform. Pre-launch. Goal: surpass Ticketmaster.com.au, DICE.fm, Eventbrite.com.au, Humanitix.com.

POSITIONING (locked, memory entry 1): COMMUNITY-FIRST, never culture-first. Community = who you associate with: heritage, music genre (hip hop, reggae, house), faith, sport, scene. Open and inclusive. Genre = a community (ties to genre discovery doc). Platform OPEN to ALL events. The word "culture/cultural/cultures" is being swept to "community" across the platform. Only exception: locked tagline "Every culture. Every event. One platform." Banned: diaspora, friends-launch, culture-first.

## 2. Tools and environment

- Stack: Next.js 16.2.2 (Turbopack), React, Supabase Sydney (project gndnldyfudbytbboxesk), Tailwind v4, Stripe (TEST mode, sandbox "Eventlinqs"), Resend, Upstash Redis, Mapbox, Vercel Pro.
- Production: https://www.eventlinqs.com . Repo: github.com/eventlinqs/eventlinqs-app
- CANONICAL CLEAN MAIN WORKTREE: C:\Users\61416\OneDrive\Desktop\EventLinqs\eventlinqs-app (others -tab-a/b/c, -hardening, -sentry-fix, -admin, -genre are working copies)
- CC is on Claude Opus 4.8, effort high/xhigh, bypass permissions on, started in each tab with `claude --dangerously-skip-permissions`.
- Three CC tabs run in parallel. Lawal pastes between them and the chat.
- DISK WAS CRITICALLY FULL earlier (hit 0.18 GB). Cleared to 3.6 GB by deleting regenerable junk (.next caches, genre worktree node_modules, k6/load-test files). Main node_modules kept. OneDrive syncing node_modules/.next is the root cause of disk pressure - worth excluding those from OneDrive sync later. npm cache (~3.2 GB) is still clearable via `npm cache clean --force` if more space needed.

## 3. CURRENT STATE OF THE THREE TABS (as of this handoff)

- TAB A: just received the "verify and fix the ENTIRE create-event-to-purchase flow in one pass" prompt (find ALL schema mismatches at once, not one at a time, then prove the flow end to end). This is the HIGH PRIORITY task - it unblocks Lawal's first real purchase. Awaiting its report.
- TAB B (culture-to-community sweep): Phase 1 (visible copy only) is DONE and green on PR #57. Nav "Cultures" -> "Communities" and all visible copy swept. Tab B is asking whether to prepare a coordinated CLAUDE.md update to match the swept code. Deferred layers (NOT done this round): routes (/cultures -> /communities + redirects), code identifiers (CultureSlug type, src/lib/cultures modules), and the database layer (a cultures table + events.culture_primary/sub_culture columns). These are a separate planned migration. First Nations/Acknowledgement-of-Country "culture" usage and real org names (e.g. "Italian Cultural Institute") were correctly LEFT UNTOUCHED. Three bonus bugs flagged: "diaspora-pop" banned word in cultures/data.ts, stale Pexels v1 hero-image slugs (newer heritages return null images), stale "14 cultures" counts (should be 21).
- TAB C (genre data-layer build): PARKED. Hit the full-disk wall, wrote src/lib/genres/data.ts + a unit test before stopping. Branch feat/genre-data-layer in worktree eventlinqs-app-genre. NOT to resume until the create-to-purchase flow is proven. When resumed: CC writes migration SQL only; Lawal runs `supabase db push --linked` + `npm run db:types` from his authenticated PowerShell (locked rule, never CC, never MCP, never Dashboard SQL). Then CC builds routes (/music/[sub]/[city], /artists/[slug]), event-form genre+artist attachment, follows table, RLS tests. Spec: docs/GENRE-DISCOVERY-FOUNDATION-SPEC.md. Visual browse page waits for designer Rizwan.

## 4. THE IMMEDIATE GOAL (what everything is driving toward)

Prove the money-to-ticket flow end to end with a real test purchase. Status: the live DB still shows 0 completed purchases / 0 tickets ever. The platform has never had a purchase complete end to end. Lawal is the first person to run the full create-and-publish-and-buy path.

Progress so far on this:
- Lawal created his own organisation (auto-promoted his account from attendee to organiser).
- He completed Stripe Connect onboarding in TEST mode (skipped the ID step, which test mode allows). Payouts now show ACTIVE: "Accept ticket payments: Enabled", "Receive payouts: Enabled". His org CAN take money now.
- He built a paid event ("Test DANCE event", 1 tier, AUD 65, 100 capacity) and reached Review & Publish.
- BLOCKER (current): clicking Publish fails with "Could not find the 'queue_admission_rate' column of events in the schema cache." This is SCHEMA DRIFT - the event-create code writes a virtual-queue column (queue_admission_rate, and likely queue_open_at) that does not exist in the live DB. PR #52 had already noted these columns don't exist. Tab A's current task is to fix ALL such mismatches in the create-to-purchase flow at once and prove the flow works.

NEXT once Tab A fixes it and deploys: Lawal publishes the event, buys a ticket with test card 4242 4242 4242 4242 (expiry 12/30, any CVC), and confirms the four proofs: purchase completes, QR ticket at /t/[code], ticket in /tickets, confirmation email arrives. THAT is the milestone.

## 5. KNOWN UX GAPS FOUND IN LIVE TESTING (real, logged, to fix before launch)

- NO visible "Create event" entry point for logged-in users (you have to know the URL). Fix: add "Create event" link in main nav (header + mobile), visible to all logged-in users. DECIDED: hero stays "Get tickets" (matches Ticketmaster "Find tickets"), create-event goes in NAV not hero. A prompt covering this + dashboard-for-all-users + cover-image-upload-broken + dead-end-Stripe-message + homepage clutter cleanup was prepared.
- Cover image upload on event-create step 4 shows a broken-image icon instead of preview - real bug.
- The "Connect Stripe..." red message on Review & Publish has NO action button - should link to /dashboard/payouts.
- Homepage may have placeholder/editorial clutter (NAIDOC doc text) Lawal wants removed from the hero.

## 6. RECENT MERGED WORK (don't redo)

- PR #51: pricing_rules schema-drift fix (checkout crash). PR #52: regenerated database.ts from live schema + 12 drift fixes + types-drift CI guard (non-blocking via continue-on-error; needs SUPABASE_ACCESS_TOKEN secret to run in CI - Lawal chose not to add it yet). PR #54: organiser-Stripe payment gate (paid event with unconnected organiser shows "tickets not yet on sale") - merged, live, production-verified. PR #55: CLAUDE.md NON-NEGOTIABLE COMPETITIVE AND QUALITY STANDARDS section + culture-first->community-first reconciliation - merged. PR #56: Lighthouse scoped to UI-only PRs (kills false-failure emails on docs/config PRs; required check still reports a fast pass) - open/verified, merge when ready. PR #57: Phase 1 culture->community visible-copy sweep - green.
- Sentry working in production. PO Box 141 NEWCOMB VIC 3219 secured. All 5 socials registered. Designer Rizwan Ali (Pakistan, Upwork) engaged, M1 paid, on Eid break - send Figma scope addendum (browse/discovery page + genre/artist chips) when he returns, never during Eid.

## 7. NON-NEGOTIABLE RULES (now also hard-wired in CLAUDE.md)

- Surpass Ticketmaster/DICE/Eventbrite/Humanitix; Playwright side-by-side benchmark on UI PRs at 1440/375.
- Every PR: typecheck 0, lint 0, vitest pass, build pass, Lighthouse 95+ desktop AND mobile (production build), axe 0, Playwright 3 viewports, all CI green, NO `gh pr merge --admin` except declared emergency.
- Robustness: verified in production before called done.
- No em/en-dashes (hyphens, colons, pipes only); Australian English; no exclamation marks; community-first never culture-first.
- All migrations via `supabase db push --linked` from Lawal's PowerShell. Never CC, never MCP (read-only), never Dashboard SQL.
- CC never improvises design; inherits tokens from src/app/page.tsx, src/app/events/[slug]/page.tsx, docs/DESIGN-SYSTEM.md. Authoritative scope: docs/EventLinqs_Scope_v5.md.

## 8. COMMUNICATION STYLE (critical)

- Direct, honest, NO sugar-coating. Lawal is intensely frustrated by recurring bugs and back-and-forth; he swears when frustrated (venting at the situation, not abuse). Stay steady, accountable, non-submissive.
- When sending him into ANY manual flow, give EVERY exact value/click up front (e.g. Stripe test data: ABN 51824753556, BSB 110000, account 000123456, card 4242 4242 4242 4242). Never make him come back to ask for details.
- Distinguish CC's auto-generated bottom suggestion lines from Lawal's actual instructions (he flags which is which).
- Don't tell him to wait/slow down when parallel work is possible. He works multi-tab and ships fast.
- One clear recommended next action per response. Tight prose. Visual-first when it helps.
- Be honest that "Ticketmaster never breaks" is a myth; the real bar is "fails gracefully, alerts instantly, can't repeat known bugs."
- Honest framing on bugs: he is the FIRST person running these flows end to end, so first-run bugs are expected; each one fixed is one a real organiser never sees. But never claim a flow is smooth without it being verified.

## 9. New chat first-message prompt

```
You are continuing work on EventLinqs, my pre-launch Australian community-first ticketing platform. Read this status file in my repo before doing anything else:

docs/STATUS-2026-05-30.md

Important: you do NOT have live access to my machine, my browser, or my Claude Code (CC) tabs. You give me prompts to paste into CC; I paste results back. I am the bridge. Always give clear numbered steps. When you send me to PowerShell, give the cd line for my working directory first. When you tell me to start Claude Code, give the full command: claude --dangerously-skip-permissions. When you send me into any manual flow, give me EVERY exact value and click up front - do not make me come back to ask.

After reading the file, give me a one-paragraph confirmation that you have the full picture (including the three tab states and the current Publish blocker), then tell me the single most important next action. Default-recommend: get Tab A's create-to-purchase fix proven and deployed, then I publish my test event and run the first real purchase. Do not invent context. If anything conflicts with your memory entries, the memory entries win unless older than this file (dated 30 May 2026).
```
