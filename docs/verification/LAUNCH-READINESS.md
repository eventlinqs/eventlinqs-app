# LAUNCH READINESS

Generated by `scripts/verify/launch-readiness.mjs` on 2026-09-09. Do not hand-edit this file:
`scripts/guards/launch-readiness-honest.mjs` re-renders it on every build and fails when the two disagree.

Close-out L5: one row per L1 item, with its state, the evidence path and the date driven.
Every row PASS, or it is not launch ready.

## VERDICT: NOT LAUNCH READY

4 of 17 rows PASS. 13 are OWNER BLOCKED. 0 FAIL.

An OWNER BLOCKED row blocks the launch exactly as hard as a FAIL. It is a separate state only
because it carries the one sentence that ends the blocking, which a bare FAIL would hide.

## What Lawal must supply, and what each unblocks

| Need | Rows it unblocks |
|---|---|
| Approval to create one test organiser account and one test event on PRODUCTION, because every organiser journey below writes to the live database and L1 requires them driven there rather than on TEST. | 1, 2, 3, 5, 6, 7, 13, 15 |
| Approval to put one real card through a low-price live event and refund it, because it is real money on the live Stripe account and no other path proves the buyer journey end to end. | 9, 10, 11, 12 |
| Apply the pending migration with `npm run migrate:production`, because production is one migration behind this tree and until it lands the fixed release cannot reach production, so a read of the live screens reads the old code. | 17 |

## The 17 rows

| # | Group | L1 requirement | State | Evidence | Driven |
|---|---|---|---|---|---|
| 1 | ORGANISER | Sign up at /signup, verify email, sign in. | **OWNER BLOCKED** | - | - |
| 2 | ORGANISER | Create an organisation, then create an event at /dashboard/events/create with a real venue address, and confirm coordinates are saved and NOT null. | **OWNER BLOCKED** | - | - |
| 3 | ORGANISER | Add tiers and pricing, add a discount code, publish. | **OWNER BLOCKED** | - | - |
| 4 | ORGANISER | The event appears on /events/[slug], on browse, on its city page, and on any community it belongs to. | **PASS** | `docs/verification/launch-readiness/appearance-2026-09-09.json` | 2026-09-09 |
| 5 | ORGANISER | Generate the Launch Kit and confirm every card renders with real ink. | **OWNER BLOCKED** | - | - |
| 6 | ORGANISER | Pause, unpause, archive, restore, and delete a zero-sales event. | **OWNER BLOCKED** | - | - |
| 7 | ORGANISER | View attendees, orders, and the GST report. | **OWNER BLOCKED** | - | - |
| 8 | ATTENDEE | Find the event from the homepage without knowing the URL. | **PASS** | `docs/verification/launch-readiness/discovery-2026-09-09.json` | 2026-09-09 |
| 9 | ATTENDEE | Buy a ticket end to end with a real card on a low-price test event, receive the confirmation email, and see the ticket at /account/tickets and /t/[code]. | **OWNER BLOCKED** | - | - |
| 10 | ATTENDEE | Refund that order from /dashboard/events/[id]/refunds and confirm the money returns and the ticket is voided. | **OWNER BLOCKED** | - | - |
| 11 | ATTENDEE | Squad or group purchase path, and a waitlist join. | **OWNER BLOCKED** | - | - |
| 12 | DOOR | Scan the issued ticket at /scan/[eventId]. It validates once and refuses the second time. Prove the offline path and the multi-scanner path. | **OWNER BLOCKED** | - | - |
| 13 | PAYOUT | The organiser payout path resolves and reports correctly at /dashboard/payouts. | **OWNER BLOCKED** | - | - |
| 14 | PLATFORM | Every route enumerated from src/app returns its expected status on production. Zero unexpected 404s. Zero 500s anywhere. | **PASS** | `docs/verification/launch-readiness/route-sweep-2026-09-09.json` | 2026-09-09 |
| 15 | PLATFORM | Every transactional and confirmation email actually sends and renders. | **OWNER BLOCKED** | - | - |
| 16 | PLATFORM | axe-core zero on every public surface. | **PASS** | `docs/verification/launch-readiness/axe-2026-09-09.json` | 2026-09-09 |
| 17 | PLATFORM | A HUMAN READ of the five launch screens - the homepage, browse at /events, an event detail page, /pricing and /organisers - at 390, 768 and 1440. Read for what a status code cannot see: unrendered markup, duplicated text, invisible controls, and elements colliding. The route sweep is NEVER reported as covering this. | **OWNER BLOCKED** | - | - |

## Row by row

### 1. Sign up at /signup, verify email, sign in.

**OWNER BLOCKED**

Needs: Approval to create one test organiser account and one test event on PRODUCTION, because every organiser journey below writes to the live database and L1 requires them driven there rather than on TEST.

Where it HAS been driven: Driven end to end on TEST through the real signup form on a local production build (close-out C10-G1, 8 September 2026).

### 2. Create an organisation, then create an event at /dashboard/events/create with a real venue address, and confirm coordinates are saved and NOT null.

**OWNER BLOCKED**

Needs: Approval to create one test organiser account and one test event on PRODUCTION, because every organiser journey below writes to the live database and L1 requires them driven there rather than on TEST.

Where it HAS been driven: Driven on TEST at 390, 768 and 1440 through the real create wizard, including the venue pick writing venue_geocode_source back (close-out C1 and C9).

### 3. Add tiers and pricing, add a discount code, publish.

**OWNER BLOCKED**

Needs: Approval to create one test organiser account and one test event on PRODUCTION, because every organiser journey below writes to the live database and L1 requires them driven there rather than on TEST.

Where it HAS been driven: Driven on TEST: tiers and pricing in the create wizard, the discount claim path in scripts/verify/discount-claim-drive.mjs, and add-ons in close-out C10-G2.

### 4. The event appears on /events/[slug], on browse, on its city page, and on any community it belongs to.

**PASS**

Driven anonymously on production against a real published event, with the slug taken from the platform own sitemap and every onward page reached from links on the event page rather than from a typed URL. This row needs no write, which is why it is PASS while the rows around it are not.

### 5. Generate the Launch Kit and confirm every card renders with real ink.

**OWNER BLOCKED**

Needs: Approval to create one test organiser account and one test event on PRODUCTION, because every organiser journey below writes to the live database and L1 requires them driven there rather than on TEST.

Where it HAS been driven: Eighteen cards across six channels proven to be decodable JPEGs at their published sizes carrying real ink, from a running server (close-out C3, 6 September 2026).

### 6. Pause, unpause, archive, restore, and delete a zero-sales event.

**OWNER BLOCKED**

Needs: Approval to create one test organiser account and one test event on PRODUCTION, because every organiser journey below writes to the live database and L1 requires them driven there rather than on TEST.

Where it HAS been driven: The whole state machine driven on TEST at 390, 768 and 1440, including the 410 and 404 rules and the money-records delete refusal (close-out C13, docs/EVENT-LIFECYCLE.md).

### 7. View attendees, orders, and the GST report.

**OWNER BLOCKED**

Needs: Approval to create one test organiser account and one test event on PRODUCTION, because every organiser journey below writes to the live database and L1 requires them driven there rather than on TEST.

Where it HAS been driven: The three surfaces answer on production to an anonymous request with a redirect to login, which is the correct anonymous answer and is not the signed-in surface (route sweep, /dashboard/events/[id]/attendees, /orders, /dashboard/reports/gst).

### 8. Find the event from the homepage without knowing the URL.

**PASS**

Driven anonymously on production at 390, 768 and 1440 by clicking only: the homepage was loaded, an event link was found on it and followed, and the page it reached was confirmed to be a real event page. No URL was typed after the homepage.

### 9. Buy a ticket end to end with a real card on a low-price test event, receive the confirmation email, and see the ticket at /account/tickets and /t/[code].

**OWNER BLOCKED**

Needs: Approval to put one real card through a low-price live event and refund it, because it is real money on the live Stripe account and no other path proves the buyer journey end to end.

Where it HAS been driven: Driven end to end on TEST with Stripe test cards, including the webhook, the confirmation email and the bearer ticket (scripts/verify/paid-purchase-webhook-e2e.mjs, ticket-email-proof.mjs).

### 10. Refund that order from /dashboard/events/[id]/refunds and confirm the money returns and the ticket is voided.

**OWNER BLOCKED**

Needs: Approval to put one real card through a low-price live event and refund it, because it is real money on the live Stripe account and no other path proves the buyer journey end to end.

Where it HAS been driven: Driven on TEST across the refund surfaces including the double-refund and orphan-inventory drills (scripts/verify/refund-dashboard-e2e.mjs, double-refund.mjs).

### 11. Squad or group purchase path, and a waitlist join.

**OWNER BLOCKED**

Needs: Approval to put one real card through a low-price live event and refund it, because it is real money on the live Stripe account and no other path proves the buyer journey end to end.

Where it HAS been driven: Driven on TEST: the squad path including expiry and the double-refund drill, and the waitlist bridge (scripts/verify/squad-expire-double-refund-drill.mjs, waitlist-bridge-e2e.mjs).

### 12. Scan the issued ticket at /scan/[eventId]. It validates once and refuses the second time. Prove the offline path and the multi-scanner path.

**OWNER BLOCKED**

Needs: Approval to put one real card through a low-price live event and refund it, because it is real money on the live Stripe account and no other path proves the buyer journey end to end.

Where it HAS been driven: Driven on TEST in full, including the offline IndexedDB path and two devices reconciling, which is build-brief B1 and B2 (scripts/verify/offline-door-schema-verify.mjs, door-realtime-verify.mjs).

### 13. The organiser payout path resolves and reports correctly at /dashboard/payouts.

**OWNER BLOCKED**

Needs: Approval to create one test organiser account and one test event on PRODUCTION, because every organiser journey below writes to the live database and L1 requires them driven there rather than on TEST.

Where it HAS been driven: Driven on TEST across the funds-holding model including reserve, refund and dispute (scripts/verify/payout-e2e.mjs, funds-holding-integrated.mjs).

### 14. Every route enumerated from src/app returns its expected status on production. Zero unexpected 404s. Zero 500s anywhere.

**PASS**

Re-driven on the current release: 211 requests over 77 static pages, 55 dynamic pages, 48 static handlers and 12 dynamic handlers. No server error, no error boundary inside a 200, no soft 404, no undeliberate 404. Dynamic values came from the platform own sitemap and from anchors harvested off the pages the sweep already drives, never from a guess.

### 15. Every transactional and confirmation email actually sends and renders.

**OWNER BLOCKED**

Needs: Approval to create one test organiser account and one test event on PRODUCTION, because every organiser journey below writes to the live database and L1 requires them driven there rather than on TEST.

Where it HAS been driven: Rendered and sent on TEST across the transactional set; the production sender is configured and the post-deploy alert path was proven to deliver on 8 September 2026 (close-out H2.4).

### 16. axe-core zero on every public surface.

**PASS**

Re-run on the current release across every public URL that answered 200 to an anonymous visitor, at 390 and 1440, at every impact level. The URL list was enumerated from the route sweep own results rather than typed.

### 17. A HUMAN READ of the five launch screens - the homepage, browse at /events, an event detail page, /pricing and /organisers - at 390, 768 and 1440. Read for what a status code cannot see: unrendered markup, duplicated text, invisible controls, and elements colliding. The route sweep is NEVER reported as covering this.

**OWNER BLOCKED**

Needs: Apply the pending migration with `npm run migrate:production`, because production is one migration behind this tree and until it lands the fixed release cannot reach production, so a read of the live screens reads the old code.

Where it HAS been driven: Read at 390, 768 and 1440 on a local production build of this tree, which is how the composed-cover crop defect was found while its own assertion was green (close-out UX1.4). It cannot be read on PRODUCTION as this release yet, because production is one migration behind and still serves the code that carries the six defects.

## What this report does not claim

A row that is OWNER BLOCKED is not a row that was never built or never tested. 13 of them carry
a line saying where the journey HAS been driven, and in every case that is TEST or a local
production build rather than production. L1 asks for production, so production is what the state
reflects. The gap is an approval, not an absence of work, and it is exactly 3 approvals wide.
