# Roast ledger: UX6 closed on the preview, 12 September 2026

Written from the literal text of CLOSE-OUT.md's UX6 body, the run brief's
condition for UX6, and the standing rules. The ledger rows were written before
the proofs were read; the verdicts after.

Evidence roots: `C:\dev\EVIDENCE\UX6\preview-a90c085a\` (the closing drive),
`C:\dev\EVIDENCE\UX6\preview-0a195454\` (the drive that found the defect, the
probes, the patch), `C:\dev\EVIDENCE\UX6\` (the 10 September legs).

## Phase 1 and 2: the requirement ledger, adjudicated

| # | Requirement, verbatim or near it | Verdict | Evidence |
|---|---|---|---|
| 1 | UX6.1 BLOCKER. The payment summary is cropped off the right edge at 390 wide. The buyer cannot see the total they are about to pay | MET | preview-a90c085a/drive-run.txt: `paid/3-checkout-details @ 390 ... 0 clipped ... total AUD 53.73`, `paid/5-payment-step @ 390 ... 0 clipped ... total AUD 53.73`; 390/paid-5-payment-step-viewport.png read by eye |
| 2 | UX6.2 BLOCKER. Multiple checkout boxes do not fit the mobile grid | MET | same run: `0 clipped` on all 13 surfaces at 390, box-level assertion (scripts/verify/lib/viewport-fit.mjs) |
| 3 | UX6.3 BLOCKER. Clipped content is unreachable; where it cannot fit it must scroll inside its own container | MET | same run, the box-level assertion that can actually fire, 0 faults; the three exemptions each named in the log |
| 4 | UX6.4 Guest ticket recovery must not require an account | MET | preview run: `free/6-ticket-view @ 390/768/1440` opens the bearer link in a fresh context, HTTP 200, no session; email branch held by tests/unit/email/guest-ticket-recovery.test.ts (7) and proven on the console transport in the 10 September drive (C:\dev\EVIDENCE\UX6\drive-run.txt) |
| 5 | Fix 1. Drive every checkout and ticket surface at 390, 768 and 1440 and capture each: event page, ticket select, checkout, payment, confirmation, ticket view, /tickets | MET | 13 captures per width at 390, 768 and 1440 (`ls` counts 13, 13, 13) plus 11 chrome captures; `payment step: 3 measured, 0 NOT EXERCISED`. The confirmation, ticket view and /tickets are the free path's, which completes a real order; the paid path stops at the payment step by design (a completed card payment is launch item L1.9, driven on production) |
| 6 | Fix 2. Assert scrollWidth <= innerWidth at each width; any surface failing fails the build | MET | gate step `checkout-viewport` PASS 217s on the push of a90c085a (push-attempt.log lines 50094 to 53924); `doc.scrollWidth 390/390` on every surface |
| 7 | Fix 3. Assert the order total sits inside the viewport box at 390 and its text is non empty | MET | `total AUD 53.73` on checkout details, checkout filled and the payment step at 390; `$0.00` on the free confirmation; `[data-order-total]` asserted by judgeSurface |
| 8 | Fix 4. Register both as blocking guards, proven to fail as well as pass | MET (10 September) | grid-track-cannot-blow-out and buyer-total-is-marked, registered, six drills; still in the 111 that PASS on this push |
| 9 | Fix 5. UX6.4 copy and routing | MET (10 September) | src/lib/email/order-confirmation.ts; row 4 |
| 10 | Reversal condition: await document.fonts.ready; never weaken | MET | every measurement awaits fonts.ready (proof script, `measure()`); no tolerance, no exemption added in this pass |
| 11 | Run brief: on a READY Vercel preview built from a pushed commit, driven at 390 on that preview, including the payment step | MET | dpl_7eXtiJX56iNs4erdwYuXqHzA2eRv READY, built from a90c085a (origin at a90c085a, 0 ahead, 0 behind), serving TEST (348 references to vkapkibzokmfaxqogypq in the HTML); the drive at 390 measured the payment step (`paid/5-payment-step @ 390`), and at 768 and 1440 as well |
| 12 | Run brief: fix every defect you find before starting the next task | MET, with one thing outside this repository named | Found and fixed: the scroll-anchoring drag on the payment step (c4086acf), the proof's picker choosing an unsellable event (a90c085a). Reported and NOT fixable here: axe critical `aria-required-children` on Stripe's own payment-method selector inside Stripe's iframe at 390 and 1440 (a `<select>` under a role that forbids one); it is Stripe's markup and the drive counts it (`2 violation(s) inside third-party frames, reported and not judged`) |
| 13 | Completion law 2: code built, typechecked, linted, no silent catches | MET | tsc exit 0 (preview-0a195454/tsc.txt), eslint 0 (eslint.txt), the push gate's lint, typecheck and build steps PASS; no-silent-catch among the 111 guards PASS |
| 14 | Completion law 3: real tests added, canary raised in the same commit | MET | tests/component/checkout-payment-step.test.tsx (4), canary 379/4602 to 380/4606 in c4086acf; the suite step PASS on the push |
| 15 | Completion law 4: the invariant held by something proven red and green | MET, by a test and a driven assertion rather than a new registered guard, and said so | RED: the component test with PaymentStep present and the fix removed fails exactly the three tests that hold the fix and passes the fourth (preview-a90c085a/component-test-red-2.txt: `3 failed | 1 passed`); the file restored, `git diff --quiet` clean. RED: the drive's new assertion against the still-deployed pre-fix preview of 0a195454: `the Payment heading sits -762px from the top of a 844px viewport (scrollY 890)` (assertion-red-on-0a195454/drive-run.txt). GREEN: both on a90c085a. No separate guard was registered: a source-level guard would only duplicate the component test, and the assertion runs in the registered gate step wherever a Stripe key exists |
| 16 | Completion law 5: driven at 390, 768 and 1440 with screenshots under C:\dev\EVIDENCE\UX6\ | MET | preview-a90c085a/390, 768, 1440 (13 each), 1024, 1100, 1280, 1366; probe/390-mobilectx-top-viewport.png and 390-mobilectx-stripe-frame.png show the Stripe fields painted |
| 17 | Completion law 6: full regression green after the item | MET | the push of a90c085a: all 15 gate steps PASS including lighthouse (1684s, on mains); 111 guards; suite 380/4606 |
| 18 | Completion law 7: committed, Australian English, no trailers, pushed | MET | c4086acf and a90c085a accepted by the commit-msg hook; 0 trailers, 0 dashes in the messages (byte-pattern grep); origin at a90c085a |
| 19 | Standing: no em or en dashes, no exclamation marks in user-facing copy, banned word absent, no competitor named in public copy | MET after one correction | 0 em or en dashes in the added lines of 4d0fda21..a90c085a (byte-pattern grep); no exclamation mark in added tsx strings; the banned word appeared 3 times in a TEST's example table names (tests/unit/guards/types-cover-migrations.test.ts, the rename case) and is replaced with neutral names in the closing commit; no competitor named |
| 20 | Standing: TEST-only writes; production never written | MET | every drive and probe ran against TEST through the preview; the one production action was a READ (`supabase gen types`) and the stale-reservation sweep ran on vkapkibzokmfaxqogypq only (test-sweep.txt names the ref) |
| 21 | Standing: DESIGN-LOCK | MET, with the two non-visual attribute changes named | The h3 gained `ref`, `tabIndex={-1}` and `outline-none` (a programmatic focus target, no visible ring on a heading); the step wrapper gained `[overflow-anchor:none]`. No colour, size, spacing, copy or layout changed; the payment branch was extracted into PaymentStep with identical markup (diff read: the same class strings moved) |
| 22 | Housekeeping: move the item body to CLOSE-OUT-DONE.md, one line left behind | MET in the same pass | close-out-move-ux6.py under preview-a90c085a; CLOSE-OUT.md carries the one-line entry |

## Phase 3: the adversarial pass

- Silent drops: none found. Every row above is named in the report and the ledger.
- Interpretation drift: one candidate, stated. "Including the payment step" was read as the payment SURFACE, measured and captured with a live Stripe frame, not as a completed card payment. CLOSE-OUT lists "payment" among the surfaces to drive; a completed real-card purchase is launch item L1.9 and is driven on production, not on a preview. The report says this in as many words.
- Match versus surpass: not a design brief; no competitor claim is made.
- Unverifiable claims: "the buyer is looking at the top of the payment step" is falsified by the assertion that fired on the old preview (scrollY 890) and passed on the new (scrollY 0, heading 128px down); "Stripe painted" is falsified by the 45s wait that reports a miss; both were tested both ways.
- Generic test: the defect and the fix are specific to this checkout's step transition; no generic copy was added.
- AI-tell sweep: 0 em dashes, 0 en dashes, 0 exclamation marks in user-facing strings, 0 tell words in added copy; the banned word's 3 test-string hits corrected.
- Regression sweep (DESIGN-LOCK): row 21.
- Founder-cost test: nothing handed to the founder; the stale TEST reservations were released by running the product's own sweep rather than asking; issue #150 closed with a pointer.
- Evidence visibility: captures at named paths for every width, the timeline in probe-390.txt, the red proofs in named files.

## Phase 4: the gate

Not met: 0. Partial: 0. Adversarial findings unresolved: 0.

ROAST GATE: PASSED
Requirements: 22. Met: 22. Partial: 0. Not met: 0.
Adversarial findings: 0 unresolved.
Ledger: docs/roast/ux6-preview-2026-09-12.md
