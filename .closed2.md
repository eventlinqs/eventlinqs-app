
--------------------------------------------------------------------------------
## A SCREEN-READER LABEL ESCAPED THE BOX THAT SCROLLS IT, AND SEVEN ADMIN SCREENS
## RENDERED AT 69 PER CENT ON A PHONE. 19 September 2026.
--------------------------------------------------------------------------------

ITEM: not a numbered CLOSE-OUT item. Lane C's named priority order is DONE and
verified by lane A, AQ3 is closed, and the standing instruction is to keep taking
defects. This one was found by READING the 390 screenshot from the item before
it rather than its report: the capture was 569 pixels wide on a 390 viewport.

COMMIT: 04e17a10 on lane/c-ux.

ACCEPTANCE, ONE LINE PER CRITERION WITH THE EVIDENCE THAT PROVES IT.

1. THE CAUSE IS ESTABLISHED BY MEASUREMENT AND BY A CONTROL, NOT BY READING. MET.
   `sr-only` is position:absolute, and an absolute element is clipped by an
   ancestor's overflow only when that ancestor is its CONTAINING BLOCK.
     the scroller               clientWidth 340, scrollWidth 720, overflow-x auto
     the sr-only label          left 568, right 569, position absolute
     documentElement.scrollWidth 569 against innerWidth 390
     `position: relative` set live on the scroller -> 390
   /admin/orders has byte-identical table markup, an identical ancestor chain,
   and no sr-only in its cells: it measured 390. Removing every `nextjs-portal`
   from the DOM left 569 unchanged, so the dev overlay was ruled out rather than
   assumed innocent.
   EVIDENCE: C:/dev/EVIDENCE/VIEWPORT-WIDTH/red.txt, the escapee lines naming
   `<label class="sr-only"> right=594 out of div class="overflow-x-auto ..."`.

2. EVERY AFFECTED ROUTE IS MEASURED BEFORE AND AFTER, AT 390, 768 AND 1440. MET.
   BEFORE: 8 admin routes failed, /admin/users 569, /admin/events 594,
   /admin/organisers 605, /admin/flags 624, /admin/pricing 633, /admin/staff 691,
   /admin/marketplace 573. AFTER: 166 of 166 checks pass across 29 public and 27
   admin routes at three widths.
   EVIDENCE: red.txt and green.txt, drive.json beside each.

3. WHAT A PERSON SEES IS ASSERTED, NOT INFERRED. MET. A mobile-emulated context
   reports `window.innerWidth` 390 on /admin/users, /admin/events and /pricing,
   where it read 569 and 594 before. The screenshots are read rather than
   counted: mobile-admin-users.png shows the screen full width and legible.
   EVIDENCE: green/mobile-admin-users.png, mobile-admin-events.png,
   mobile-pricing.png and the three `.layout-viewport` checks.

4. A REGISTERED BLOCKING GUARD, PROVEN TO FAIL AS WELL AS PASS. MET.
   scripts/guards/sr-only-cannot-escape-a-scroller.mjs, registered in
   run-guards.mjs, 175 guards green. Two clauses: the PREMISE (globals.css does
   not redefine sr-only, so the rule is still true) and the RULE (in a file that
   paints an sr-only, every horizontal container is a containing block). Six
   drills, 6 of 6 fired, tree green after restore.
   EVIDENCE: C:/dev/EVIDENCE/VIEWPORT-WIDTH/drills.txt.

5. THE GUARD WAS CAUGHT BY ITS OWN DRILL AND REWRITTEN. MET, and this is the
   line worth reading. The first version bounded each container's JSX subtree and
   reported OK on the whole tree; the drills then said
   "DID NOT FAIL ... guard PASSED on a violating tree. It is not actually
   guarding" for BOTH pages the defect was found on. On /admin/users the label is
   inside a `UserRow` function further down the same file, rendered in by
   `{rows.map(...)}`, so it is not lexically inside the container at all. The
   trigger is the FILE now, and the guard's header states what that costs rather
   than hiding it.
   EVIDENCE: the guard's own header, and drills.txt, which now fires on both.

6. THE WIDER RULE WAS MEASURED BEFORE IT WAS CHOSEN. MET. The file-level trigger
   named 7 more containers; a "subtree contains a table" trigger would have named
   21 across three lanes' territories. The 7 were taken and every one was read
   before being changed: six wrap a table, and the seventh is a figure whose only
   absolute child already sits in its own `relative` wrapper.

7. TESTS. MET. tests/unit/a11y/sr-only-cannot-escape-a-scroller.test.ts holds
   nine cases and EXECUTES the guard against synthetic trees, red and green,
   including the sibling-component shape that defeated the first version, four
   alternative containing-block tokens, a vertical scroller that must be left
   alone, and a file with no sr-only, which is what /admin/orders was.
   tests/unit/dashboard/main-column-shrinks.test.ts gains one case for the
   containing block; its other two were RELAXED, because they pinned the ORDER of
   utility classes and went red against a correct change. Canary raised 489/6368
   to 490/6378 in the same commit, MEASURED.
   EVIDENCE: gate-suite.txt.

8. FULL REGRESSION GREEN. MET. typecheck PASS, lint PASS, copy PASS, guards PASS
   (175), types-drift PASS, suite PASS (490 files, 6378 tests, 0 failed, 0
   skipped). Lighthouse, build and production-parity NOT run; they are lane A's.
   EVIDENCE: gate-typecheck.txt, gate-lint.txt, gate-copy.txt, gate-guards.txt,
   gate-types-drift.txt, gate-suite.txt under C:/dev/EVIDENCE/VIEWPORT-WIDTH.

BENCHMARK VERDICT: not taken, and the reason is stated rather than skipped. Seven
of the nine surfaces are internal operator screens behind a role check and 2FA,
and neither Ticketmaster nor Eventbrite publishes its admin console. The floor
this was judged against is harder than a capture: the document either fits the
phone or it does not.

WHAT IS NOT CLAIMED. Two surfaces were changed and NOT driven, and they are named
rather than folded into the pass: the header search overlay's tablist and the
city picker dialog are opened by a control, so no route measurement reaches them.
Both are held by the guard and by the fact that the change cannot move a painted
pixel. The organiser dashboard tables (attendee, payouts, GST) were likewise not
driven: they need an organiser session with rows, which is a different journey
from this item.

TWO NON-DEFECTS RECORDED SO NOBODY RE-INVESTIGATES THEM. /artists and /gigs
answer 404 on TEST because `artist_showcase` and `gig_board` are off; the drive
reports them as NOTES and never as passes. And killing `next dev` mid-write
leaves `.next/dev/types/routes.d.ts` truncated, which fails typecheck with 60
TS1005 errors in a generated file; the repair is removing `.next/dev/types` as a
unit, not the `.next` cache.

FOUNDER STEPS (Law 10): NONE. No migration, no production write, no dashboard
step. The proof is one command:
  BASE=http://localhost:3200 node --env-file=.env.local \
    scripts/verify/mobile-viewport-width-drive.mjs --out C:/dev/EVIDENCE/VIEWPORT-WIDTH \
    --admin --shot /admin/users --shot /admin/events --shot /pricing
