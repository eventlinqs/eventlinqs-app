# Audit screenshots are heavy (~40 MB), rarely diff-meaningful, and
# can be regenerated by re-running the phase scripts against the same
# commit. Kept on disk for local review during the audit window; not
# committed to git. The JSON evidence files and markdown report are
# the durable artefacts.
screenshots/
